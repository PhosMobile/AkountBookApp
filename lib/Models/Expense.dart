class Expense {
  String id, name, description, price, date, businessId, userId;
  int quantity;
  Expense(this.id, this.name, this.description, this.quantity, this.price,
      this.date, this.businessId, this.userId);

}
